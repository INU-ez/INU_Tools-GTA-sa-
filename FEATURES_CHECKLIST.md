# INU Tools (GTA SA) — Чеклист фич с кнопками и пайплайнами

Перепроверено по коду (`ui/panels.py` + операторы) и документации. Для каждой фичи: **Где** — панель/меню и реальная подпись кнопки; **Пайплайн** — как пользоваться. Отмечай `[x]` по мере проверки в Blender.

> Панель по умолчанию — N-сайдбар 3D-вида → вкладка **GTA Tools**, если не указано иное.
> ⚠️ Ниже есть секция **«Расхождения и скрытое»** — фичи, которых в UI фактически нет или они отключены.

---

## 🎮 Мульти-игра (III / VC / SA)

- [ ] **Переключение игры (SA / VC / III)**
  - **Где:** GTA Tools (верх панели) → ряд «SA» / «VC» / «III»
  - **Пайплайн:** 1) Нажми игру. 2) Переключаются RW-версия, COL1/2/3, IMG VER1/VER2, лимиты ID, набор флагов IDE.
- [ ] **Переключение платформы (PC / Mobile)**
  - **Где:** GTA Tools → ряд «PC» / «Mobile» (над рядом игр)
  - **Пайплайн:** 1) Выбери PC (vanilla RW + D3D-текстуры) или Mobile (Native Data PLG + PVRTC/ETC1). 2) DFF/TXD пишутся в формате платформы.
- [ ] **Авто-детект игры при импорте + предупреждение**
  - **Где:** автоматически внутри «Импорт» / drag&drop
  - **Пайплайн:** 1) Импортируй файл. 2) Игра определяется по RW-версии / магии COL / числу колонок IPL / VER2. 3) На пустой сцене вкладка авто-переключается; на заполненной — WARNING «файл = X, а сцена = Y».
- [ ] **Кросс-игровая трансляция флагов IDE**
  - **Где:** автоматически при «Add to IDE» и полном экспорте IDE
  - **Пайплайн:** 1) Импортируй объект из игры A. 2) Переключи сцену на игру B. 3) Биты флагов транслируются A→B; набор чекбоксов флагов в панели тоже игро-зависимый.
- [ ] **Validate Scene — предупреждения кросс-игры** (см. «Проверка перед экспортом»)

## 📦 Экспорт / Импорт (ядро)

- [+] **Импорт (мульти-выбор + фильтр форматов)**
  - **Где:** «Экспорт / Импорт» → кнопка «Импорт»; либо File → Import → «INU Import (.dff/.col/.cst/.txd/.ide/.ipl)»
  - **Пайплайн:** 1) «Импорт». 2) В сайдбаре браузера под «Показывать форматы:» отметь DFF/COL/CST/TXD/IDE/IPL (правит фильтр). 3) Выдели один/несколько файлов. 4) Импорт ровно выбранных (порядок TXD→DFF→COL→CST→IDE→IPL), прогресс модальный, ESC отменяет.
- [+] **Экспорт всех выделенных (папка / в IMG / «Также в IDE/IPL»)**
  - **Где:** «Экспорт / Импорт» → кнопка «Экспорт»
  - **Пайплайн:** 1) Выдели модели → «Экспорт». 2) Отметь DFF / COL / LOD / TXD / CST. 3) Опц.: «Также в IDE / IPL (пути из панели)», «Один DFF (машина/пед)», «Дописать в существующий TXD». 4) Выбери папку — или внизу «All → IMG» (пишет в .img из настроек). 5) Имена файлов = имена моделей.
- [+] **Экспорт одного DFF (машина / пед)**
  - **Где:** File → Export → «INU Export DFF — single model (vehicle / ped)»; либо галочка «Один DFF (машина/пед)»
  - **Пайплайн:** 1) Выдели часть машины/педа. 2) Экспорт: вся иерархия корневого фрейма + коллизия → один .dff.
- [+] **Экспорт DFF по моделям (несколько → несколько файлов)**
  - **Где:** оператор `gtatools.export_dff_models` (через общий групповой движок)
  - **Пайплайн:** 1) Выдели несколько моделей. 2) Разбивка на группы по имени → каждая в `<base>.dff` (+ `LOD<base>.dff`).
- [+] **COL импорт / экспорт**
  - **Где:** «Импорт»/«Экспорт» с галочкой «COL» (+ «Пустая коллизия», + «Библиотека COL»: чекбокс и имя, по умолч. `collision`)
  - **Пайплайн:** 1) Импорт: отметь .col. 2) Экспорт: включи «COL»; «Библиотека COL» пишет все COL в один файл (multi-entry, матч с DFF по ID).
- [+] **TXD импорт / экспорт / дозапись (append)**
  - **Где:** «Импорт»/«Экспорт» с галочкой «TXD» + «Общий TXD» (имя, по умолч. `textures`) + «Дописать в существующий TXD»
  - **Пайплайн:** 1) Импорт: отметь .txd. 2) Экспорт: «Дописать…» (merge) обновляет текстуры сцены в файле, сохраняя чужие; иначе перезапись.
- [+] **CST импорт / экспорт + drag&drop**
  - **Где:** галочка «CST» в «Импорт»/«Экспорт»; **drag&drop** .cst в вьюпорт; операторы `gtatools.import_cst`/`export_cst` через F3
  - **Пайплайн:** 1) Перетащи .cst в 3D-окно. 2) Или отметь .cst в «Импорт». 3) Экспорт — галочкой «CST» (те же настройки, что COL).
- [+] **Меню File → Import / Export** — те же операторы из стандартных меню Blender.
- [+] **Диагностика выделения (DFF/LOD/COL счётчики)**
  - **Где:** «Экспорт / Импорт», верхний бокс (всегда виден)
  - **Пайплайн:** показывает «Выделено: N меш(ей)» + DFF/LOD/COL (имя, `+N` для батча, галочка/крест) перед экспортом.
- [+] **Пайплайн + DFF Flags**
  - **Где:** «Экспорт / Импорт» → ряд пайплайнов (Vehicle/Building/D-N/Ped) + разворот «DFF Flags»
  - **Пайплайн:** 1) Выбери пайплайн. 2) Раскрой «DFF Flags»: Normals/Light/Modulate Color/Set Material Alpha/UV1/UV2/Day/Night/Light Beam. 3) Несовместимые с пайплайном флаги краснеют; **Night на UV-аним модели даёт предупреждение** (ломает UV-анимацию в retail SA).

## 🗺️ IDE / IPL / IMG

- [ ] **Панель IDE / IPL / IMG** (свёрнута по умолчанию) — боксы IDE/IPL/IMG со статусом активного объекта.
- [ ] **Выбор файлов IDE/IPL/IMG (пикер + фильтр расширений)**
  - **Где:** в каждом боксе — иконка-папка 📁 в шапке
  - **Пайплайн:** 1) 📁 → выбери файл. 2) Путь показан короткой меткой; при неверном расширении «Add» предупреждает.
- [ ] **Add to IDE (блок ID=0, авто-LOD, трансляция флагов)**
  - **Где:** бокс IDE → «Add»
  - **Пайплайн:** 1) Выбери IDE (📁) → выдели меши → «Add». 2) Пишет в ВЫБРАННЫЙ файл (приоритет над запомненным target). 3) `model_id == 0` блокируется (id 0 = игрок). 4) Флаги транслируются под игру, LOD дописывается.
- [ ] **Add to IPL (lod_index, восстановление связи, FLA realInterior)**
  - **Где:** бокс IPL → «Add»
  - **Пайплайн:** 1) Выбери IPL (📁) → выдели → «Add». 2) `id == 0` блокируется. 3) DFF пишется первым, затем LOD — `lod_index` DFF = индекс строки LOD (потеря → WARNING). 4) Если есть `real_interior != 0` — формат FLA (12 колонок), иначе vanilla 11.
- [ ] **Del из IDE / IPL** — боксы IDE/IPL → «Del» (удаляет записи выделенных из выбранного файла).
- [ ] **Импорт / Экспорт IDE и IPL** — боксы → «Import» / «Export» (+ счётчики содержимого файла в шапке).
- [ ] **Секции IPL (cull / grge / enex / pick / cars / auzo / jump / occl / zone)**
  - **Где:** «IDE / IPL / IMG» → разворот «Дополнительно (IPL)» → «Импорт» / «Экспорт» (+ «Заменить Empty на модели»)
- [ ] **Синхронизация с файлами + Multi-IPL Sync**
  - **Где:** разворот «Синхронизация с файлами» → «Обновить» / «Отвязать» / «Проверить»; вложенный «Sync несколько IPL» → «Добавить» / «Очистить»
  - **Пайплайн:** 1) «Обновить» тянет файл→сцена, «Отвязать» снимает связи, «Проверить» сверяет. 2) Для карты из нескольких .ipl добавь пути в «Sync несколько IPL».
- [ ] **IMG импорт / экспорт / удаление**
  - **Где:** бокс IMG → «Импорт из IMG» / «Экспорт в IMG» / «Удалить из IMG» + тумблеры «Skip LOD» / «TXD» / «COL»
- [ ] **Блок «имя > 24 символов» при экспорте в IMG**
  - **Пайплайн:** экспорт в IMG отклоняется, если имя записи > 24 байт (LOD-имя переполняется первым) — «обрежется и не совпадёт с IDE/IPL».
- [ ] **Пересобрать IMG (компакт)**
  - **Где:** бокс IMG → «Пересобрать IMG (компакт)»
  - **Пайплайн:** архив атомарно перестраивается, мёртвое место от перезаписей освобождается; отчёт «N записей, X МБ».
- [ ] **Список файлов IMG** — Properties → Scene → «INU Tools» → разворот «Файлы IMG» → «Обновить список» (счётчики DFF/COL/TXD).
- [ ] **Импорт карты по региону**
  - **Где:** панель «INU Tools» (workflow карты) → поле региона + «Import Map»
  - **Пайплайн:** 1) Задай Game Root, выбери район. 2) Фильтры «Без LOD/2DFX/TXD/коллизии», «Группировать по IPL». 3) «Scan» бинарных/текстовых IPL → отметь нужные. 4) Сохрани .blend, «Извлечь ресурсы» → «Import Map».
- [ ] **Object IDE / IPL (пер-объект)**
  - **Где:** «Object IDE / IPL» (активный меш); Properties → Object → «INU Tools: Model»
  - **Пайплайн:** Model ID, Draw/LOD Dist, Interior, LOD, Flags (под игру); «Применить к выделенным (N)»; «Разрушаемый (Breakable)» + Break Force.

## 🔢 Менеджер ID моделей + тип модели

- [ ] **ID Manager** — под-панель со статистикой (Свободных/Занятых/Следующий), поиском, списком занятых (крест = освободить) и свободных ID.
- [ ] **Auto-Assign (Назначить)**
  - **Где:** «ID Manager» → бокс «Присвоить выделенным:» → «Назначить»
  - **Пайплайн:** 1) Выдели меши с ID=0. 2) «Назначить» — раздаёт свободные ID (DFF→LOD, COL пропускаются), обходя занятые; LOD = `LOD<base>`.
- [ ] **Assign From (С ID…) + «Пропускать занятые»**
  - **Где:** «ID Manager» → «С ID...» (диалог)
  - **Пайплайн:** 1) Выдели → «С ID...» → Start ID. 2) Тумблер «Пропускать занятые»: ВКЛ — как авто; ВЫКЛ — строго по порядку, при пересечении «конфликтов: N». Свои ID не считаются занятыми.
- [ ] **Очистить ID выделенных** — «Очистить выделенные» (освобождает слот только если ID больше не занят другим объектом).
- [ ] **База ID и сервис** — разворот: «Sync» (добавить ID сцены), «Из игры» (читать IDE по Game Root), «Создать ID» (321–19999), «Расширить FLA» (диалог Count), «Освободить фантомы» (GC), «Очистить всё», «Открыть файл ID».
- [ ] **Пресеты ID** — ряд пресета: ➕ (new, опция «Скопировать с активного») / ✎ (rename) / ➖ (delete, кроме default).
- [ ] **Определение типа модели (DFF / LOD / COL, LOD без регистра)**
  - **Где:** авто (`core/model_classify`); ручной override — «INU Tools: Model» → «Экспортировать как».
  - **Пайплайн (порядок правил):** 1) `_SHA`→COL; 2) явный суффикс/префикс override; 3) `inu.type`=COL/SHA→COL; 4) токен «lod» где угодно **без учёта регистра**→LOD; 5) есть текстура→DFF, нет→COL.
- [ ] **Batch Set Type** — «Проверка» → ряд «Тип:» → OBJ / COL / SHA / NON (ставит `inu.type` + переименовывает).
- [ ] **BBox Mode**
  - **Где:** панель «INU Tools» (workflow карты) → «BBox: ON/OFF»
  - **Пайплайн:** 1) Включи — тяжёлые меши показываются как bounding-box (быстрый вьюпорт на больших картах). 2) Повторный клик выключает. *(Работает на всех мешах; не лагает в Edit Mode.)*

## 🎨 Материалы

> Панель: Properties → Material → **GTA Material** → вкладки **SURFACE** (COL) и **EFFECTS** (остальное).

- [ ] **RW-затенение (ambient / specular / diffuse)**
  - **Где:** EFFECTS → «RW-затенение:» → «Фоновое (ambient)» / «Зеркальное (specular)» / «Рассеянное (diffuse)»
  - **Пайплайн:** крути коэффициенты RW-освещения (round-trip в DFF; масштабируют яркость при запечённом свете).
- [ ] **Цвет / прозрачность / режим блендинга**
  - **Где:** EFFECTS → «Цвет / прозрачность:» → «Цвет (RGBA)» + «Режим прозрачности»
  - **Пайплайн:** 1) Свотч RGBA (RGB → material color, A → alpha). 2) «Режим прозрачности» для превью.
- [ ] **Текстура: имя + фильтрация + адресация U/V + маска**
  - **Где:** EFFECTS → бокс «Текстура:» → «Имя» / «Фильтрация» / «Адресация U»+«V» / «Маска»
  - **Пайплайн:** 1) Выбери картинку (превью). 2) «Имя» авторитетно для экспорта. 3) Фильтр/адресация round-trip'ятся в DFF.
- [ ] **GTA-эффекты: env map / bump / reflection / specular / dual-tex / UV-анимация**
  - **Где:** EFFECTS → блок тумблеров: «Карта окружения» / «Карта высот» / «Отражение материала» / «Зеркальный материал» / «Blend Mode (Src/Dst)» / «UV Анимация»
  - **Пайплайн:** включай нужный тумблер → появляются его поля (текстуры, коэффициенты, для UV-аним — имя + Scroll/Keyframe).
- [ ] **Быстрые пресеты материала (Стекло / Хром / Краска / Сброс)**
  - **Где:** EFFECTS → «Быстрые пресеты:» → «Стекло» / «Хром» / «Краска» / «Сброс»
  - **Пайплайн:** клик проставляет `mat.inu.*` (VEHICLE_GLASS / CHROME / VEHICLE / GENERIC-сброс).
- [ ] **Копировать настройки материала на выделенные**
  - **Где:** EFFECTS → «Копировать на выделенные»
  - **Пайплайн:** 1) Источник + активный материал. 2) Shift-выдели приёмники. 3) Клик — RW-затенение/фильтр/эффекты/цвет копируются (COL и слот машины исключены).
- [ ] **Слот цвета машины (carcols)** — EFFECTS → «Слот цвета машины:» dropdown + «Применить SA Vehicle defaults».
- [ ] **Типы поверхностей COL (Surface Types)**
  - **Где:** SURFACE → «ID» + 🔍 (поиск из 179 типов) + «Дневной свет» / «Ночной свет» / «Яркость»
  - **Пайплайн:** 1) 🔍 → набери `wood` → выбери тип. 2) Задай Day/Night (0–15) и яркость.

## 💡 Prelight (вершинные цвета)

> Путь: GTA Tools → **Lighting** → **Prelight** (+ подпанели Инструменты / Advanced Settings / Post-Processing / Prelight COL / Листва).

- [ ] **Day/Night атрибуты + Adjust Color (V-offset на атрибут)**
  - **Где:** Prelight → ряды «Day» / «Night» → селектор + поле «V» + [+]/[−]; «Day → Night» / «Night → Day»
  - **Пайплайн:** 1) [+] создаёт атрибуты. 2) Клик по имени — активный. 3) «V» → Enter — сдвиг яркости немедленно (per-object, переживает re-bake).
- [ ] **Запекание (Запечь / Запечь с тенями)**
  - **Где:** Prelight → «Свет (8 ламп)» / «Солнце» → «Запечь» / «Запечь с тенями»; параметры в «Advanced Settings»
  - **Пайплайн:** 1) Создай Day/Night → выбери активный. 2) «Свет (8 ламп)» вокруг объекта. 3) «Окружающий»/«Интенсивность»/«Гамма». 4) «Запечь» (без теней) или «с тенями» (raycast).
- [ ] **Запечь поверх (аддитивно)** — «Запечь поверх» / «…с тенями»: добавляет вклад только новых ламп (наслаивание проходов).
- [ ] **Солнце (Prelight Sun)** — «Солнце»: направленная лампа `Prelight_Sun` сверху-спереди; повтор — удалить.
- [ ] **Заливка Day/Night (Fill Colors)** — Инструменты → «Залить одним цветом:» → «День»/«Ночь»/«Только выделенные»/«Применить».
- [ ] **Рассеять цвет (Scatter Color)** — Инструменты → «Рассеять цвет:» → «Сила»/«Дальность»/цвет из кисти → «Применить» (falloff вокруг выделения).
- [ ] **Покраска нескольких объектов (Merge / Split)** — Инструменты → «Рисовать по нескольким:» → «Объединить» (прокси) → крась → «Разъединить» (запись обратно).
- [ ] **Резак света → топология (Light Cutter)** — Инструменты → «Свет → топология:» → «Создать резак» (Цилиндр/Сфера, радиус, +Кольцо) → «Нарезать по резаку» (радиальный градиент × цвет лампы).
- [ ] **Пост-обработка** — Post-Processing → «Сглаживание»/«Контраст»/«Яркость»/«Гамма»/«Подтянуть тени» (каждое с «Применить»).
- [ ] **COL Light (vcol → 0–15)** — Prelight COL → «Превью COL Light» / «Запечь COL Light» + диапазоны Day/Night (Мин/Макс).
- [ ] **Прилайт листвы / деревьев** — Листва / Дерево → «Прилайтить крону» (Сфера/Цилиндр, градиент) + «Запечь цвет» / «Сброс».
- [ ] **Превью вертекс-альфы по сцене** — Prelight → «Альфа вершины (сцена)» (показывает только слоты с реальной альфой Day/Night < 255) + 🗑 очистка.
- [ ] **VC Layer System — Слои Vertex Color (Day/Night, лимит 10)**
  - **Где:** Prelight → секция «Слои Vertex Color (BETA)» → «Live preview» / стеки «Слои Day»/«Слои Night» [+]/[−]/[▲]/[▼] / «Рисовать» / «→ База» / «Перекрасить выделенные…»
  - **Пайплайн:** 1) [+] → `VCL_D_Layer_1` (лимит 10/стек). 2) «Рисовать» → Vertex Paint слоя. 3) Режим (Нормаль/Умножение/Добавить/Вычесть) + opacity. 4) «Live preview» — композит (auto-flatten при экспорте). 5) «→ База» — в обычный атрибут.
- [ ] **Пресеты Prelight** — шапка Prelight: «Применить» (load ✓) + dropdown + ✏ (rename) + [+] (save) + [−] (delete) + ⬆ (overwrite).

## ✨ 2DFX-эффекты

> Панель: GTA Tools → **2DFX Effects**. Детальные настройки эффекта — **Object Data Properties → «GTA SA: 2DFX»**.

- [ ] **Создание 2DFX** — «Создать эффект» → «2DFX» (свет) / «Частица» / «Ped Attractor» / «Блик солнца» (Empty на 3D-курсоре, для Light/Particle сразу превью).
- [ ] **Привязка к модели (сразу нескольких)** — выдели меш + Shift-выдели пустышки → иконка-цепочка (`attach_2dfx`): ВСЕ выделенные парентятся к мешу; разорванная цепочка = отвязать.
- [ ] **Настройки эффекта в свойствах пустышки (свет)** — «GTA SA: 2DFX» → секции «Свойства света» (Цвет/Корона/Дальность/Радиус/Имя короны) / «Поведение» / «Тень» / «Флаги».
- [ ] **Флаги света (побитовые тумблеры)** — «GTA SA: 2DFX» → «Флаги»: именованные кнопки (Check Obstacles, Without Corona, AT_DAY/AT_NIGHT, мигания, светофор…), каждая XOR-ит бит.
- [ ] **Пресеты 2DFX** — «GTA SA: 2DFX» → «Пресеты:» dropdown + «Применить».
- [ ] **Применить настройки к выделенным пустышкам** — 2DFX Effects → «Применить настройки»: копирует inu + `2dfx_*` на остальные выделенные, превью авто-перестраивается.
- [ ] **Тумблер линий-связей (пунктир)** — 2DFX Effects → чекбокс «Связи (пунктир)» (overlay родитель→ребёнок).
- [ ] **Счётчик привязанных + «Отвязать все»** — (активен меш) бокс «Привязанные 2DFX: N» → «Отвязать все».
- [ ] **Источник текстур эффектов (particle.txd)** — 2DFX Effects → статус .txd + иконка-папка (ассеты игры в аддон не входят).
- [ ] **Обновление превью / авто-ребилд при дублировании** — при Shift+D / Ctrl+C+V фоновый таймер (~0.6 c) достраивает превью-детей; иконка «обновить превью» форсирует.

## 🔥 Particle Effects (effects.fxp)

> Тип «Частица» (тот же «Создать эффект»). Редактор — **Object Data Properties → «GTA SA: 2DFX»** при `PARTICLE`. Нужен **Game Root** (пишет в `<game>/models/effects.fxp` + `.bak`).

- [ ] **Привязка частицы + выбор эффекта** — «Свойства частицы:» → поле «Эффект» (системы из FXP) → привязка цепочкой.
- [ ] **Редактирование эмиттера** — секции «Спрайт и смешивание» / «Цвет (start→end)» / «Размер» / «Эмиссия» / «Физика» / «Система».
- [ ] **Редактор кривых (keyframes)** — «Кривые (keyframes)» → «Выбрать кривую...» → правь ключи (+/−) → «Записать кривую в effects.fxp».
- [ ] **Мульти-эмиттерное переключение** — ряд «◄ Emitter N/M ►» (перечитывает из FXP, предупреждает о несохранённом).
- [ ] **Живая симуляция в вьюпорте** — тумблер «Симуляция» (до 64 квадов/пустышка, ~30 Гц, учёт rate/life/speed/force/friction/bounce).
- [ ] **Создание / удаление эффекта (запись в effects.fxp)** — «Свойства частицы:» → «+» (создать) / «−» (удалить) / «обновить»; первая запись делает `.bak`.
- [ ] **Сохранение правок** — «Сохранить в effects.fxp» (диалог: имя = клон / Overwrite; пишет только изменённые поля).

## 🌀 Animated Map Objects

> Панель: GTA Tools → **Анимации** → таб **«Object»**. Kam's-стиль: Empty-rig (root + pivot'ы), меши парентятся к pivot'ам.

- [ ] **Настройка rig'а пипеткой (авто)** — «Animated Map Object» → пипетка + радио «Куда»: ткни статику (root), затем анимируемую часть (pivot).
- [ ] **Ручной парентинг + мультичасти** — «Настройки: <pivot>» → «К pivot» / «К root» / «+Pivot» (свой BoneID + Action).
- [ ] **Вращение pivot'а (Auto)** — тумблер «Авто/Вручную» + Ось / «В обратную сторону» / «Оборотов за цикл» / «Длительность (кадров)» (keyframes цикла пересчитываются сами).
- [ ] **Валидация rig'а** — «Validate» (root+pivot, BoneID, Action с ключами, припарентенные меши).
- [ ] **Экспорт DFF+IFP+IDE одним кликом** — «DFF+IFP+IDE» (диалог: Папка/Имя/TXD/Model ID/Draw dist/файл IFP/формат ANPK-ANP3/IDE entry) → пишет `<base>.dff` + `<base>.ifp` + опц. IDE anim-строку.

## 🦴 Персонажи (Skinned DFF)

> Панель: GTA Tools → **Анимации** → таб **«Character»**. Импорт/экспорт модели — через «Экспорт / Импорт».

- [ ] **Импорт скина (авто-скелет)** — «Импорт» .dff: из HAnimData+SkinData строится Armature + vertex groups.
- [ ] **Экспорт скина (один DFF)** — таб Character → «Экспорт скина (один DFF)…»; либо пайплайн «Ped» (has_skin, гасит day/night vcol + MatFX).
- [ ] **Импорт / экспорт / добавление IFP** — таб Character → «Импорт» / «Экспорт» (ANPK/ANP3 + тумблер **«Только активную анимацию»**) / «Добавить» (merge).
- [ ] **Применение анимации + Preview** — поле «Анимация» + «Применить» + тумблер «Preview» (живой предпросмотр); корзина = удалить активный Action.
- [ ] **IK Rig (control-bones)** — «IK Rig» → «Add IK Rig» (+ «Root motion (walk/run)») → анимируй за контроллеры → «Bake & Clear IK».
- [ ] **IK-дополнительно** — «Дополнительно»: «Пол» + «Коллизия», IK color/размер, тумблеры видимости контролов, «Проверить round-trip», «Batch папка…».
- [ ] **Weight Paint: швы на split-вершинах** — (в Weight Paint) «Объединить для покраски» → крась → «Применить и вернуть швы» / «Откатить» (не Ctrl+Z в merge).
- [ ] **Batch Import IFP из папки** — «Дополнительно» → «Batch папка…» (фильтр имени / индекс / count → на NLA-трек).
- [ ] **Настройка анимации** — «Настройка анимации»: «Исправить кватернионы (sign-flip)», «Сгладить между выделенными ключами».

## 🧰 UV / Текстуры

- [ ] **UV Tools (рандомайзер сетки)** — UV-редактор → N → GTA Tools → «Рандомизатор UV сетки»: «Показать сетку», Колонки/Ряды, 3×3-выравнивание, «Рандом» / «Привязать».
- [ ] **UV-анимация (scroll / keyframe + предпросмотр)**
  - **Где:** UV-редактор → N → GTA Tools → «UV Анимация» (дубль в свойствах материала)
  - **Пайплайн:** 1) Тумблер «UV Анимация». 2) SCROLL (Speed U/V + «Длительность») или KEYFRAME («Вставить ключ» / «Очистить»). 3) **Пробел** — предпросмотр.
- [ ] **Texture Browser (индекс TXD)** — «Проверка» → «Текстуры (TXD)»: источник DAT/Folder/Custom + «Cross-ref с IDE» → «Сканировать» → превью (ленивый DXT-декод) + «Used by N».
- [ ] **Bitmaps Manager (Менеджер текстур)** — панель «Менеджер текстур» → «Сканировать» + меню «Текстуры» (Найти/Скопировать/Дубликаты/Неиспользуемые) + меню «Материалы» (Проверка/Очистка/Сортировка).

## 🔥 Texture Baking

> Путь: **UV/Image-редактор → N → GTA Tools → Texture Bake** (Cycles).

- [ ] **Размер / Padding / AA** — верх панели: «Размер» (пресет 2^n) / «X»/«Y» / «Padding» / «АА» (Выкл/2×/4× суперсэмплинг).
- [ ] **Режимы запекания** — бокс «Запекание» → «Режим»: **UV → UV** / **Hi → Low** (пары `_hi`/`_low` или DFF/LOD, Cage/Max Ray) / **Камера** (ортокамера → текстура с альфой, для импостеров).
- [ ] **Стек слоёв + 18 blend-режимов** — «Добавить слой» (AO/Diffuse/Diffuse Lit/Shadow/Bevel/Normal/Emission/Emission Light GI) → список → «Выбранный слой»: «Режим»/«Прозрачность»/«Контраст»/«Гамма»/«Обесцветить».
- [ ] **Дополнительно** — подпанель: «Samples» / «Свет (экспозиция)» / «Bevel радиус»+samples / «Cage»+«Max Ray».
- [ ] **Bake + Превью + Сохранить как** — «Bake» на слое → живой композит; «Показать текстуру» (flat-emission превью); «Сохранить как» → сводит 👁-слои в sRGB PNG (опция ½/¼/⅛).

## ✅ Check / Валидаторы

- [ ] **Проверка геометрии** — «Проверка» → «Проверка вершин» (loose) / «Проверка N-gon» (5+ вершин). *(Лимит атомика 65535 верт. — как INFO «Большие меши» в свипе.)*
- [ ] **Проверка перед экспортом (Validate Scene)**
  - **Где:** «Экспорт / Импорт» → под-панель «Проверка перед экспортом» → «Проверить сцену»
  - **Пайплайн:** 1) «Проверить сцену». 2) Баннер (N ✗/⚠/i) + группы: Paintjob, Кватернионы, **UV-аним × Night**, Пары _ok/_dam, Сирые LOD/COL, Дубликаты model_id, Пустые/Большие меши, Материал без текстуры, Суффиксы, Scale, Light Beam ASI. 3) «Перейти» / «Нормализовать» / «Исправить»; «X» очищает. *(Modulate Color — УДАЛЁН, заменён на UV-аним × Night.)*
- [ ] **Cleanup / Сортировка материалов** — переехало в «Менеджер текстур» → «Сортировка материалов» + Find/Remove Unused + Find Duplicates.
- [ ] **File Scanner + Lint Profiles** — «Проверка» → «Анализ карты/файлов» → режим «FILES» → папка + типы DFF/COL/TXD + «Профиль» + «ERR» → «Сканировать» → детали по строке → «Сохранить .txt».
- [ ] **Map Analyzer (кросс-проверка IDE/IPL)** — та же панель → режим «MAP» → источник DAT/FOLDER/CUSTOM + «Проверять модели в IMG» + «Профиль» → «Проанализировать» → сводка E/W/Σ.

## 💧 Water / 🛣️ Path

- [ ] **Water IO** — панель «Water»: «Импорт»/«Экспорт» (water.dat) / «Добавить воду» + «Параметры воды» (flag, скорость X/Y/Z, «Волны») → «Применить»; «Привязка к сетке (x4)» / «Сшить края».
- [ ] **Пути IPL (авто / пешеходные)** — «Пути» → «Пути (paths.ipl)»: «Импорт»/«Экспорт»/«Создать путь»; тип Авто/Пешеходный, лимит 12 точек; в Edit Curve — «Переключить Roadblock», меню «Светофор».
- [ ] **Ж/д пути + маркеры станций** — «Пути» → «Ж/д пути»: «Импорт»/«Экспорт»/«Создать»; в Edit Curve «Станция (вкл/выкл)»; в Object Mode «Обновить маркеры станций».
- [ ] **Скомпилированные Nodes (NODES*.DAT) + FLA4** — «Пути» → «Скомпилированные (NODES)»: «Импорт»/«Экспорт», «Геометрия путей» (тоггл), curve-workflow (Меш→Curves / Curves→.dat, +TL/RB/CO); FLA4 — галка при File → Export → Export Path Nodes.

## 🔌 Интеграции / Advanced

- [ ] **Сборка Asset Library** — вкладка N-панели «GTA Library» → «Собрать Asset Library»: 1) «Извлечь ресурсы» (чипы готовности) → 2) «Папка библиотеки» + опции → «Разрешить сборку» → «Собрать»; «Перегенерировать превью».
- [ ] **Import / Export Map** — панель «INU Tools»: «Import Map» / «Export Map» (нужен сохранённый .blend); бинарный IPL — File → Export → Export IPL → «Binary (bnry)».
- [ ] **Breakable Objects** — Properties → Object → «Object IDE / IPL» → «Разрушаемый (Breakable)» + «Break Force» (DFF chunk 0x253F2FD) → экспорт DFF.
- [ ] **Машины: масштаб / damage / paintjob** — панель «Машины»: «Экспорт машины (один DFF)…», «Масштаб машины…», damage («Создать _dam», «Показать: OK/Dam/Оба», «Проверить пары»); Paintjob — GTA Material → «Раскраска 1»/«Раскраска 2».
- [ ] **Roadblocks & Traffic Lights** — «Пути» → Edit Curve на path_ipl → «Переключить Roadblock» + меню «Светофор»; либо curve-workflow «+TL/RB/CO».
- [ ] **X Radar Maker** — панель «X Radar Maker»: «Папка»/«Сетка»/«Размер»/«Высота» → меню «Генерировать» → «Упаковать в TXD».
- [ ] **Itera Tools 3** — «Lighting» → «Itera Tools 3»: нужна библиотека Itera в ассетах; «Vertex Lit Linear» / «Quickstart» / «Убрать Itera».

## 🪟 UI / Прочее

- [ ] **Плавающие GPU-окна** — иконка-окно в шапке панелей: Info (главная), I/E («Экспорт / Импорт»), Validation («Проверка»), Lighting, IDE/IPL/IMG — GPU-оверлей, таскаются/сворачиваются.
- [ ] **Горячая клавиша Shift+T** — в 3D-вьюпорте открывает/закрывает UV-редактор (единственный хоткей аддона).
- [ ] **Профили N-панели** — верх главной панели → dropdown «Профиль» + [+]/[−]/шестерёнка: `ALL` = все; сохраняй наборы видимости+порядка панелей; шестерёнка = редактор раскладки.
- [ ] **Onboarding** — панель «Поддержка»: «Docs» / «Issues» / звезда (What's New popup).
- [ ] **Preferences → Run Legacy Migrations** — Edit → Preferences → Add-ons → INU Tools → «Совместимость со старыми версиями» → «INU: Run Legacy Migrations» (4 миграции по требованию; авто-миграции при загрузке .blend отключены).

---

## ✅ Разобрано (ревизия по коду)

- [x] **Collection Export** — подтверждено: в коде НЕ существует (единственное упоминание — комментарий в `map_export.py`). Из чеклиста убрано. Ближайшее по смыслу — «Группировать по IPL» при импорте и «Библиотека COL» / «Общий TXD».
- [x] **Прокручиваемый список 2DFX** (`GTATOOLS_UL_2dfx`) — **УДАЛЁН** из кода (класс + регистрация). Нигде не рисовался. Осталось: счётчик «Привязанные 2DFX: N» + «Отвязать все».
- [x] **«Заливка граней» / «Рассеять от выделенных»** — скрытая панель `GTATOOLS_PT_vertex_paint_panel` (`poll → False`) **УДАЛЕНА**. Операторы `fill_faces`/`scatter_light` и scatter-пропсы оставлены нетронутыми: `gtatools_scatter_*` сериализуются в пресеты Prelight (`prelight_preset_ops.py`) — их удаление сломало бы пресеты. Панели-точки входа больше нет.
- [x] **Standalone Import/Export CST** — операторы `gtatools.import_cst` / `gtatools.export_cst` (только F3, дублировали галочки) **УДАЛЕНЫ**. Функции `import_cst`/`export_cst` (нужны Export All) и **drag&drop** (`gtatools.drop_cst` + FileHandler) оставлены.
- [x] **Название панели материала** — не критично (косметика). Доки исправлены: «GTA SA Material Effects» → **«GTA Material»** (4 вхождения в DOCS_rus, 6 в DOCS + якорь оглавления).
- [x] **JSON-пресеты материала** — **ВЫПИЛЕНЫ** (мёртвый код): удалён `data/material_presets.py`, EnumProperty `gtatools_material_preset`, proxy `_mat_preset_items_proxy`, callback `preset_items`/`invalidate_cache` и ветка `USER:` в операторе. Остались 8 встроенных пресетов (`apply_preset`), из них 4 на кнопках (Стекло/Хром/Краска/Сброс).
- [x] **LightMap (beta_MTA)** — решение: **оставить скрытой** (`poll → False`, код на будущее). Это НЕ превью прилайта — прилайт-превью у нас есть (Vertex Alpha Preview, live-preview VC-слоёв, Превью COL Light, материал по Пробелу). `GTATOOLS_PT_lightmap_panel` — отдельный генератор lightmap'ов под MTA-шейдер (недоделан). Рабочая кнопка «Добавить LightMap» (UV2) в Prelight — другое, не трогаем.

## ⚠️ Осталось

- [ ] **Подписи пресетов Prelight** — в docs load/overwrite перепутаны местами относительно кода (реально: «Применить» ✓ = load, ⬆ = overwrite).

---

## 🔧 Затронуто правками последней сессии (проверить в первую очередь)

- [ ] Add-to-IDE / IPL: блок при Model ID = 0 (ошибка, не пишет)
- [ ] Add-to-IPL: lod_index сохраняется при раздельном добавлении DFF / LOD; warning при потере
- [ ] Add-to-IPL: коллизия LOD `id+1` — warning
- [ ] Add-to-IDE: перевод флагов из другой игры (VC / III → SA)
- [ ] Add-to-IPL: FLA real_interior (12-я колонка) пишется при использовании
- [ ] Export в IMG: блок при имени > 24 символов
- [ ] Export All (интегрированный IDE): LOD получает `id+1`, а не 0
- [ ] Rebuild IMG: компакт + усадка архива
- [ ] draw_distance: дефолт 299, намеренные 300 не затираются
- [ ] **UV-аним × Night**: красная подсветка «Night» в DFF Flags + WARNING в свипе (ночные vcol ломают UV-аним в retail SA)
- [ ] **Modulate Color check УДАЛЁН** (был ложным срабатыванием)
- [ ] Определение типа модели: авто (DFF/LOD/COL), LOD без учёта регистра; UI суффиксов удалён
- [ ] BBox Mode: работает на всех мешах, не лагает в Edit Mode
- [ ] 2DFX: настройки в Object Data, «Применить к выделенным», attach-multiple, авто-ребилд превью при дублировании
- [ ] ID Manager: «С ID...» не уезжает со старта + тумблер «Пропускать занятые»
