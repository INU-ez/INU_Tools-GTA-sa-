__pycache__/
*.pyc
.pytest_cache/

# Build artifacts (extension zip is regenerated by dev/build_extension.ps1)
inu_tools_gta_sa-*.zip
INU_tools-*.zip

# NVTT module — copied from extras/ into addon at build time (full mode).
# Source of truth lives at extras/nvtt_compress.py outside the addon tree
# so the store build for extensions.blender.org has zero NVTT references.
INU_tools/tools/nvtt_compress.py

# E2E test runner status file — pytest exit code from inside Blender
dev/tests/.e2e_last_exit
